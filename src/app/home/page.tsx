import { HomeView } from "@/components/home-view";

// TODO: understand why we need this page for the real home page to not break...
export default async function HomePage() {
  return <HomeView />;
}
